/* type of function and variable do not match, wrong */
let
        var a : float;
        function myPrint ( n : int) : int begin
                printi(n);
                return 1;
        end;
in
        a := myPrint(a);
end

/*
print:
call, printi, n
end:

callr, a, print, a
*/
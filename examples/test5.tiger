let
        var a, b : int := 0;

        function testFunction(n: int) begin
            a := n + 1;
            printi(n);
            a := n + 1;
        end;
in
        if(a = b) then
                a := b + 2;
        else
            a:= 2;
        endif;
        testFunction(a);
end
